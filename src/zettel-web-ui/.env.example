# Copy this to .env.production.local and fill in values from terraform outputs.
# These values are safe to embed in the frontend build — they are public client IDs,
# not secrets. The actual protection is the Cognito Hosted UI and PKCE flow.

VITE_API_URL=https://your-api-gateway-id.execute-api.eu-west-1.amazonaws.com

# From: terraform output cognito_client_id
VITE_COGNITO_CLIENT_ID=your-cognito-app-client-id

# From: terraform output cognito_domain
VITE_COGNITO_DOMAIN=https://zettel-abc123.auth.eu-west-1.amazoncognito.com
